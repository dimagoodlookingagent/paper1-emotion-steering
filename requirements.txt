# Python dependencies for Paper 1 reproduction
#
# Tested with Python 3.11+ on macOS M1 (MPS) and Linux x86_64 (CUDA).
# Install: pip install -r requirements.txt

# Core ML stack
torch>=2.0           # tested with 2.11; PyTorch — MPS or CUDA backend
transformers>=4.40   # tested with 5.6; for Gemma-2-2B-IT
numpy>=1.24          # array math
scikit-learn>=1.3    # probe training (LogisticRegression)

# Plotting
matplotlib>=3.7      # figures 1-6
plotly>=5.18         # extract_calm_v2.py / extract_confident_v2.py validation plots

# Note: sentencepiece is required as a transitive dep of the Gemma tokenizer
# but is typically pulled in automatically by transformers.
